import { Story, Meta } from '@storybook/react'
import BannerBusiness from '.'

export default {
  title: 'Home/BannerBusiness',
  component: BannerBusiness
} as Meta

export const Default: Story = () => <BannerBusiness />
